# Gemstone V22 — True Bottom Mobile Navigation

V22 changes the HTML structure directly.

On phones:
- Gemstone and Logout remain in the top header.
- Home, Membership, and Profile are fixed at the bottom.
- The active page is highlighted.
- The old top menu is completely hidden.

On desktop:
- The normal top navigation remains.

No new Supabase SQL is required.

Upload the V22 files to GitHub Pages, preserve `config.js`, then close and reopen the APK or browser.
